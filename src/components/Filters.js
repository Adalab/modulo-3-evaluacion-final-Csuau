import App from './App'

{/*function Filters () {
    const handleFilterName = (ev) => {
        setFilterName(ev.target.value);
        props.handleFilterName({
            value: inputValue,
            name: inputName
        })
    };
}
export default Filters*/}